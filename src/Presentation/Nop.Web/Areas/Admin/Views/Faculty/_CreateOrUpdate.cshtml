@model Nop.Web.Areas.Admin.Models.Corporations.FacultyModel

@{
    const string hideInfoBlockAttributeName = "FacultyDetailsPage.HideInfoBlock";
    var faculty = await workContext.GetCurrentCustomerAsync();
    var hideInfoBlock = await genericAttributeService.GetAttributeAsync<bool>(faculty, hideInfoBlockAttributeName);
}

<div asp-validation-summary="All"></div>
<input asp-for="Id" type="hidden" />

<section class="content">
    <div class="container-fluid">
        <div class="form-horizontal">
            <nop-cards id="faculty-details-cards">
                <nop-card asp-name="faculty-info" asp-icon="fas fa-info" asp-title="@T("Admin.Corporations.Faculties.Info")" asp-hide-block-attribute-name="@hideInfoBlockAttributeName" asp-hide="@hideInfoBlock" asp-advanced="false">@await Html.PartialAsync("_CreateOrUpdate.Info", Model)</nop-card>
            </nop-cards>
        </div>
    </div>
</section>